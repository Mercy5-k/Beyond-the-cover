{
  "name": "beyond-the-cover-api",
  "version": "1.0.0",
  "description": "Backend for Beyond the Cover - A book explorer app",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}

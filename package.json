{
  "name": "beyond-the-cover-api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx --no-install json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": "20.x"
  }
}

